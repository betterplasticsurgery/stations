.DS_Store
CLAUDE.md

# source art — the mark ships as inline SVG inside index.html
*.png
# except the social card, which has to be a real URL for Open Graph
!og.png
_to_delete/

# marketing source — drafts and social art, not part of the deployable
marketing/

# agent pipeline — local working files
.pipeline/
.claude/
node_modules/
tests/.artifacts/
test-results/
playwright-report/
