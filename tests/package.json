{
  "name": "stations-tests",
  "private": true,
  "type": "module",
  "description": "Local regressions for STATIONS. Not a dependency of index.html and never will be.",
  "scripts": { "test": "node run.mjs" },
  "devDependencies": { "playwright": "^1.47.0", "ws": "^8.18.0" }
}
